import { FilterSubsitePipe } from './filter-subsite.pipe';

describe('FilterSubsitePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubsitePipe();
    expect(pipe).toBeTruthy();
  });
});
